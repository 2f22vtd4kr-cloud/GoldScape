# Bug hunt — EstateOfMind (2026-08-12)

## Crashes
| Severity | Issue | Status |
|----------|--------|--------|
| High | `Can't find variable: Check` on `/properties` (all `?country=` filters) | **Fixed** — `CheckCircle` + import |

## Code fixes this pass
| Severity | Issue | Fix |
|----------|--------|-----|
| Medium | `md:liquid-glass` is not a Tailwind responsive utility for a plain CSS class | Use `liquid-glass` only on sticky filter bar |
| Low | Footer privacy/terms were `href="#"` | Point to `/about#privacy` and `/about#terms` |
| Low | Type filter had ПЕНТХАУС / СТУДИЯ with zero matching listings | Removed those options |

## Confirmed OK
- App routes + wouter catch-all `/*`
- Lucide imports on main pages (after Check fix)
- `listingById` / `countryByCode` / `countryByListingCode`
- `ACTIVE_SCENE_TYPES = []` (terrain-only product rule)
- Map asset imports match usage; no duplicate listing IDs
- `lib/compare.ts`, favorites, Layout + CompareBar

## Open (not crashing)
| Severity | Issue |
|----------|--------|
| Content | Shared placeholder covers (e.g. 5× `dest-dubai.jpg`) |
| Content | Legal anchors on `/about` still need real privacy/terms copy |
| UX | Compare only via CompareBar (not main nav) — may be intentional |
| Design | Sticky filter `top-20` vs fixed nav height on some mobile widths |

## Ops
Replit must **pull latest main** after the Check fix or the catalogue crash persists on all countries.
